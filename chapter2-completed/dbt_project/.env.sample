# Redshift Serverless ワークグループのエンドポイント
DBT_REDSHIFT_HOST=<workgroup>.<account>.<region>.redshift-serverless.amazonaws.com

# ハンズオン共通のセットアップで `aws login --profile <name>` した AWS プロファイル名
AWS_PROFILE=dbt-book

# profiles.yml をプロジェクトローカルから読ませるための設定
DBT_PROFILES_DIR=.
