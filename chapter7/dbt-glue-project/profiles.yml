jaffle_shop_glue:
  target: dev
  outputs:
    dev:
      type: glue
      role_arn: "{{ env_var('GLUE_ROLE_ARN') }}"
      region: ap-northeast-1
      workers: 2
      worker_type: G.1X
      idle_timeout: 10
      schema: jaffle_shop_iceberg
      database: jaffle_shop_iceberg
      session_provisioning_timeout_in_seconds: 120
      location: "s3://{{ env_var('CH7_S3_BUCKET_NAME') }}/dbt-glue-tables/"
      datalake_formats: iceberg
      conf: >-
        --conf spark.sql.extensions=org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions
        --conf spark.sql.catalog.glue_catalog=org.apache.iceberg.spark.SparkCatalog
        --conf spark.sql.catalog.glue_catalog.warehouse=s3://{{ env_var('CH7_S3_BUCKET_NAME') }}/dbt-glue-tables/
        --conf spark.sql.catalog.glue_catalog.catalog-impl=org.apache.iceberg.aws.glue.GlueCatalog
        --conf spark.sql.catalog.glue_catalog.io-impl=org.apache.iceberg.aws.s3.S3FileIO
        --conf spark.sql.defaultCatalog=glue_catalog
